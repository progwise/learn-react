describe("AboutMe", () => {
  test("1 + 2 should be 3", () => {
    const result = 1 + 2;
    expect(result).toBe(3);
  });

  it("should return 3 for 1 + 2", () => {
    const result = 1 + 2;
    expect(result).toBe(3);
  });
});
